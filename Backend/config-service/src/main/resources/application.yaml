server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Gr3yW0lfChie/Tingeso_Evaluacion_2
          search-paths: config-data
  application:
    name: config-service
